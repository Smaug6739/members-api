import { Iconfig } from './types';
export const config: Iconfig = {
    port: 3000,
    production: false,
    database: {
        host: 'localhost',
        user: 'root',
        password: 'Io73uh96/&*',
        database: 'instant-chat'
    }
}
